{
  "name": "t3x",
  "version": "0.1.0",
  "private": true,
  "description": "T3X - Semantic extraction, diff, and merge for conversational AI",
  "workspaces": [
    "t3x-core",
    "t3x-core/sdk/javascript",
    "t3x-cli",
    "t3x-webui"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:cli",
    "build:core": "npm run build -w @t3x/core",
    "build:cli": "npm run build -w t3x-cli",
    "build:webui": "npm run build -w t3x-webui",
    "clean": "npm run clean -w @t3x/core && npm run clean -w t3x-cli",
    "test": "npm run test -w @t3x/core && npm run test -w t3x-cli",
    "lint": "npm run lint -w @t3x/core && npm run lint -w t3x-cli"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
