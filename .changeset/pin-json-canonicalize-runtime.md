---
'@t3x-dev/cli': patch
'@t3x-dev/mcp': patch
'@t3x-dev/core': patch
---

Pin `json-canonicalize` to the complete 2.0.0 artifact so independently installed runtime packages cannot resolve the incomplete 2.0.1 release.
