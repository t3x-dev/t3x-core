---
'@t3x-dev/cli': major
---

Require immutable Source/Workspace extraction and route the `yops apply` and top-level `commit` compatibility commands through canonical Transition proposal and CommitV2 authority.
