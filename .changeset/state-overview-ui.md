---
't3x-webui': minor
---

Add the first State Overview UI with inert author README, generic section summary
and scrollable committed content. Honor explicit historical commit selection in
snapshot views instead of replacing it with branch HEAD.
