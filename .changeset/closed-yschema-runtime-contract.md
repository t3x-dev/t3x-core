---
'@t3x-dev/yschema': patch
---

Close the YSchema P0 runtime contract against unknown schema fields, ignore inherited schema and candidate tree properties during normalization and validation, and execute required relation rules during validation.
