---
'@t3x-dev/yops': minor
---

Add versioned YOps recipe compiler profile metadata and a base-aware `replace-path` recipe that compiles to primitive `assert` plus `set` or `unset` operations without changing the frozen 18-op v1 runtime union.
