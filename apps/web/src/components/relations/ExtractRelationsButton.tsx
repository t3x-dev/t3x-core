'use client';

/**
 * ExtractRelationsButton is no longer functional.
 * Relations are now stored inline in commit content.relations.
 * This component is kept as a stub for backward compatibility.
 */

export function ExtractRelationsButton() {
  return null;
}
