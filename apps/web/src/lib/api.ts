/**
 * API client shim - re-exports from modular api/ directory
 *
 * This file exists for backward compatibility so that existing imports
 * like `import { ... } from '@/lib/api'` continue to work.
 */
export * from './api/index';
