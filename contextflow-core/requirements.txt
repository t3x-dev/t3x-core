# ContextFlow Core Runtime Dependencies
# Based on deterministic framework required by docs/ARCHITECTURE.zh.md

# NLP Engine (Ring 1/2/3 extractor)
spacy>=3.7.0,<4.0.0

# Chinese tokenization (Chinese Ring extractor)
jieba>=0.42.0

# YAML parsing (Bridge template loader)
PyYAML>=6.0,<7.0

# Embedding model (Draft Workflow similarity computation)
sentence-transformers>=2.2.0,<3.0.0

# LLM Provider (Draft Polish and Merge conflict resolution - optional)
openai>=1.0.0,<2.0.0

# HTTP client for Claude API
httpx>=0.25.0,<1.0.0

# Database (SQLite Indexes layer - included in Python standard library, no additional installation needed)
# sqlite3 is part of Python standard library

# Hashing and serialization (JSONL main ledger - included in Python standard library)
# hashlib, json are part of Python standard library

# spaCy model download (execute manually after installation):
# python -m spacy download en_core_web_sm
# python -m spacy download zh_core_web_sm  # Chinese support (optional)

# Note:
# 1. spaCy models need to be downloaded separately, run the above commands after installing dependencies
# 2. sentence-transformers will automatically download MiniLM model on first use
# 3. If you need to use other NLP engines (Stanza, Spark NLP), please reference docs/ARCHITECTURE.zh.md
