# Core → Cloud actual-form qualification

Cloud branch `test/schema-delivery-compatibility` in t3x-dev/t3x-cloud pins one exact nine-package Core set and protected shared Web snapshot. Cloud auth, billing, usage-history and Workspace fetching overlays are preserved. Its final PR records the exact pin; dependency: Core #1557 after #1556.

## Executed evidence

- Core production browser: native sample repair and browser-only author/review/commit/byte-exact export, 2 passed.
- Cloud real local source API/Web with disposable PostgreSQL, actual Cloud migrations and a personal Free namespace: those two journeys plus author image/README revision, history and stale writer, **3 passed**. No model keys or response mocks; the existing capacity policy executes normally.
- Cloud API: **91 files / 666 tests passed**, serial PostgreSQL fixtures. Parallel local runs encountered database initialization resource failures; no assertions were skipped in the passing run.
- Cloud Web: **280 files / 1685 tests passed**. Complete check/build, package integrity, protected Web sync and Core75/Cloud45 database/recovery contracts passed.
- Installed-package composed-route tests verify source/preview authentication, exact Commit/State export and Leaf writer 410 independently of browser development auth.

## Visual critique

Approved Overview and Studio renders were opened alongside actual Cloud screenshots. The compact author header and flexible Markdown remain separate from metadata tags and the derived summary. The blue rail shows actual revision and not-run status. Repeated sample records use named-row tables; complex objects retain generic reading. A container with children no longer falsely says Empty object. The authoring fixture uses project identity instead of assuming the standalone t3x-dev namespace.

The Cloud browser run uses explicit local development auth on loopback. Its development runtime emits a React/Next script-tag warning, visible in the screenshot badge; no pageerror or failed journey is hidden. Production builds pass. This does not qualify hosted OAuth, deployed data, external execution or recovery readiness. The fixture verifies its temporary database directory before applying Cloud migrations.

## Remaining decisions

Default-organization Team capacity is intentionally unapproved and remains denied. Approve commercial Team limits separately before offering that path. The qualification uses a personal Free account and does not change policy.

Recently published is not editor-selected. Author images are optional; editorial collection membership and media still require real curated content. No automatic deployment or universal OSS runtime validation follows from tags or samples.
