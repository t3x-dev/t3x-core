# Discover and author introduction qualification

Real production API/Web with disposable PostgreSQL; no model keys or response mocks. Commands: `node tools/full-e2e.mjs -- e2e/smoke/schema-ecosystem-starters.spec.ts e2e/smoke/state-author-editor.spec.ts` (2 passed); targeted catalog/candidate API tests (17 passed).

The author uploads a PNG made from the repository favicon, edits the description, tags and Markdown, and saves a new introduction revision. Business JSON remains byte-equivalent. Historical introduction and stale-writer conflict are exercised. Registry-only starters read exact-release Markdown without creating a Studio candidate; source/project authorization and manifest digest are checked by the read route.

Reference: ../../01-discover.png and ../../06-state-overview.png. Actual screenshots are attached alongside this report. Discover retains the two-column editorial scale, compact category/search strip and real definition glimpse. Releases with an author image use it; releases without one use bounded node/slot names. No generated illustration or fabricated execution status is substituted. Recently published is intentionally not labeled editor-selected.

State uses a small author avatar beside the description, a compact derived summary, flexible README and the existing independently scrollable renderer. The edit action now shares the title row instead of reserving an empty row. Structure and Code remain the existing views. Small-screen authoring is captured at 390 × 844.

Remaining visual difference: the approved concept has bespoke editorial imagery; the current built-in releases have no author-supplied covers. Source-derived fallback is qualified, but an editorial collection still needs actual curated content. The existing application shell and theme are preserved.
